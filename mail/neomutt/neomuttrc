set header_cache= "~/.neomutt/hcache"
set message_cachedir="~/.mutt/messages"
set certificate_file ="~/.neomutt/certificates"
set sort = threads
set send_charset="utf-8"
set edit_headers
set ssl_force_tls
set mailcap_path = "~/.neomutt/mailcap"
set editor = "nvim -c 'source $HOME/.config/nvim/init.vim'" # for some reason, neomutt doesn't load my configs
set text_flowed
set wrap=100
set tmpdir="$HOME/.neomutt/.tmp"
set timeout=10
unset markers

alternative_order multipart/mixed multipart/related text/plain
mime_lookup application/octet-stream
auto_view text/html

source "$HOME/.neomutt/keybindings"
source "$HOME/.neomutt/sidebar"
source "$HOME/.neomutt/colors"
source "$HOME/.neomutt/account_setup"
